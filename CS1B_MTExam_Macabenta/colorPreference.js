// MACABENTA, VHARDEY JOHN F.
let userColor = []; // empty array to store our inputted values

for(let i = 0; i < 3; i++){ // enter a for loop
  userColor.push(prompt("Enter 3 colors that you like.")); // pushes the value entered into oour empty array
  console.log(userColor); // update our array for every iteration
}